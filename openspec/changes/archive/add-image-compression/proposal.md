# Change: 添加基本图像压缩功能

## Why
项目需要添加基本的图像压缩功能，支持将图像压缩到指定大小（20KB以下），以满足用户对图像优化的需求，特别是在网页和应用程序中的资源优化场景。

## What Changes
- 添加图像压缩核心功能模块
- 支持输入输出格式配置
- 实现压缩到指定大小（20KB以下）的功能
- 添加命令行接口支持
- 实现质量与大小的自动平衡算法

## Impact
- Affected specs: image-compression (新建)
- Affected code: 将创建新的模块和文件，包括压缩器、配置和CLI接口